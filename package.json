{
  "private": true,
  "scripts": {
    "build:route-map-vendor": "node scripts/sync-route-map-vendor.js",
    "build:css": "cleancss -o docs/style.min.css docs/style.css",
    "build:js": "terser docs/script.js -c -m -o docs/script.min.js",
    "build:html": "node scripts/inline-css.js",
    "build:offline-snapshot": "node scripts/build-offline-snapshot.js",
    "build": "npm run build:route-map-vendor && npm run build:css && npm run build:js && npm run build:html && npm run build:offline-snapshot"
  },
  "devDependencies": {
    "@protomaps/basemaps": "^5.7.2",
    "clean-css-cli": "^5.6.3",
    "pmtiles": "^4.4.0",
    "terser": "^5.46.1"
  }
}
